Review map-extent._selectdetails property, see if it's actually used, can be deleted perhaps
Validate that map-option serializes the HTML option and this is repeated appropriately for HTML select generated by map-select.
